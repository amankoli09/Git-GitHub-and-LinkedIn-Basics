# Merge Conflict Resolution

A merge conflict happens when two branches modify the same line.

## Steps to Resolve
1. Open the conflicted file
2. Fix the conflicting code manually
3. Add the file
4. Commit the changes

git add filename
git commit -m "Resolve merge conflict"
